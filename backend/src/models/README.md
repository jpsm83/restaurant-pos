# Backend models

This folder will contain the **Mongoose models** used by the Fastify backend.

During migration we will **copy** schemas from `lib/db/models/**` (legacy Next.js backend logic) into here, keeping schema shape and refs consistent.

