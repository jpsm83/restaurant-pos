/**
 * Placeholder: business credentials settings (`/business/:businessId/settings/credentials`).
 */
export default function BusinessCredentialsSettingsPage() {
  return (
    <main className="min-h-0 flex-1 p-6">
      <h1 className="text-xl font-semibold text-neutral-900">Credentials</h1>
    </main>
  );
}
