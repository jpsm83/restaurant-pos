/** `/:userId/customer/profile` — placeholder until profile UI ships. */
export default function UserCustomerProfilePage() {
  return (
    <main className="flex min-h-0 flex-1 flex-col p-6">
      <h1 className="text-xl font-semibold text-neutral-900">Profile</h1>
      <p className="mt-2 text-sm text-neutral-600">This page is not built yet.</p>
    </main>
  );
}
